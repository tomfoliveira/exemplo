pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'echo "adicionando teste"'
        sh 'ls -l '
      }
    }
    stage('Send') {
      steps {
        sh 'ls -l'
      }
    }
  }
}